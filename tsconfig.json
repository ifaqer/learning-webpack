{
    "compilerOptions": {
        "outDir": "./build",
        // Директория, куда TypeScript будет сохранять скомпилированные JavaScript файлы
        //
        "noImplicitAny": true,
        // Запрещает использование неявного типа 'any', заставляя явно указывать типы
        // Это помогает избежать ошибок, связанных с неопределенными типами
        //
        "target": "ES5",
        // Указывает версию JavaScript, в которую будет компилироваться TypeScript код
        // ES5 обеспечивает широкую совместимость с браузерами
        //
        "jsx": "react-jsx",
        // Определяет, как обрабатывать JSX синтаксис
        // "react-jsx" автоматически импортирует функции из React (не требует import React)
        //
        "allowJs": true,
        // Позволяет компилятору TypeScript обрабатывать обычные JavaScript файлы
        // Полезно при постепенной миграции с JS на TS
        //
        "moduleResolution": "node",
        // Определяет стратегию разрешения модулей
        // "node" использует алгоритм разрешения модулей как в Node.js
        //
        "allowSyntheticDefaultImports": true,
        // Разрешает использовать синтаксис import x from 'y' даже если модуль 
        // не имеет экспорта по умолчанию (создает синтетический дефолтный экспорт)
        //
        "esModuleInterop": true,
        // Добавляет совместимость между CommonJS и ES модулями
        // Позволяет использовать import для модулей, которые используют module.exports
        //
        "baseUrl": ".",
        // Базовый URL для разрешения не относительных имен модулей
        // Точка отсчета для путей, указанных в опции "paths"
        //
        "paths": {
            "components/*": [
                "./src/components/*"
            ],
            "@/*": [
                "./src/*"
            ]
        } // Определяет псевдонимы путей для упрощения импортов
        // Позволяет использовать короткие пути вместо относительных
    },
}